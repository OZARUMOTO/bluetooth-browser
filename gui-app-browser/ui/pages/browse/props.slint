// SPDX-FileCopyrightText: 2026 Michael Totten <mike@ozaru.io>
// SPDX-License-Identifier: GPL-3.0-or-later

@rust-attr(route(path = "/browse"))
export struct BrowseProps {
    // No parameters for browse (state lives in BrowserCallbacks)
}
